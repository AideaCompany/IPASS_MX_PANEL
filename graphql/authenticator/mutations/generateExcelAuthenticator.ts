export const generateExcelAuthenticator = /* GraphQL */ `
  mutation generateExcelAuthenticator($page: Int, $limit: Int, $filters: Any) {
    generateExcelAuthenticator(page: $page, limit: $limit, filters: $filters)
  }
`
